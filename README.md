# Wiggler-Anti-AFK-
A lightweight, zero-dependency Windows utility that simulates mouse movement to prevent system idle states. Using the native SendInput hardware pipeline, Wiggler updates OS idle timers using smooth, randomized vector paths that return the cursor to its exact origin. Safe, undetected, and needs no external packages.
